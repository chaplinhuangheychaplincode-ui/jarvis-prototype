{
  "version": "2026-07-07",
  "description": "Jarvis capability manifest — only ops Jarvis can fully execute end-to-end today.",
  "ops": [
    {
      "name": "lookup",
      "description": "Look up a user's current tier, credits, quota, and account status",
      "example": "who is user@example.com",
      "cms_endpoints": ["/v1/internal/movio/user.get"],
      "write": false
    },
    {
      "name": "quota_grant",
      "description": "Grant credits or change a user's subscription tier",
      "example": "give user@example.com 500 credits on creator tier for 90 days",
      "credit_types": ["generative_credit (default)", "plan_credit", "api", "seat", "video_translate", "avatar_video", "personalized_video"],
      "cms_endpoints": ["/v1/internal/movio/gift_subscription.add", "/v1/internal/movio/gift_quota.add"],
      "write": true
    },
    {
      "name": "create_account",
      "description": "Create a new HeyGen account, optionally with a comped subscription",
      "example": "create account for new@example.com on pro tier for 30 days",
      "cms_endpoints": ["/v1/internal/create_account", "/v1/internal/movio/gift_subscription.add"],
      "write": true
    },
    {
      "name": "bulk_grant",
      "description": "Grant credits or tier to a batch of up to 100 users at once",
      "example": "bulk grant creator tier + 200 credits to: a@x.com, b@x.com, c@x.com",
      "cms_endpoints": ["/v1/internal/movio/gift_subscription.add"],
      "write": true
    },
    {
      "name": "revoke_grant",
      "description": "Cancel a subscription or expire a specific credit grant for a user",
      "example": "revoke subscription for user@example.com  |  expire quota abc123 for user@example.com",
      "cms_endpoints": ["/v1/internal/movio/gift_subscription.remove", "/v1/internal/movio/gift_quota.expire"],
      "write": true
    }
  ],
  "coming_soon": [
    {
      "name": "ent_sub_grant",
      "description": "Enterprise subscription grant with AE attribution and multi-seat support"
    }
  ]
}
